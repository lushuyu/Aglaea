"use client";

// Re-export sonner's toast API as the project toast primitive.
// Import { toast } from "@/components/ui/toast" in all callsites.
export { toast } from "sonner";
export type { ExternalToast } from "sonner";
